# Understand LTE with matlab

- `matlab` folder contains all the code
- `screenshot` contains screenshots, plus any drawings
- `report.tex` for generate the report

to generate the report

```bash
pdflatex report.tex
```
